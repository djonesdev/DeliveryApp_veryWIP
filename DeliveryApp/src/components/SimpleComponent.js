import React from 'react'
import { View, Text, Button } from 'react-native'
import { connect } from 'react-redux'


export const SimpleComponent = ({
}) => (
    <View>
        <Text>SimpleComponedsdnt</Text>
    </View>
);

  